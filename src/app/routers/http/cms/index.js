const router = require("express").Router();
// const middleware = require("@Middwares/cms/Auth");

// router.use("/auth", require("./auth.router"));
// router.use("/user", middleware, require("./user.router"));

module.exports = router;
